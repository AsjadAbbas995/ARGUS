"""SQLAlchemy-free data models mirroring ``05_DATA_MODEL.md``.

Each class maps 1:1 to a documented table. Repositories convert these to rows.
Only the milestone-relevant fields/types are present; anything added later must
first be added to ``05_DATA_MODEL.md``.
"""